import NavbarFooter from "shared/ui/navbar";

type Props = import("react-router-dom").RouteChildrenProps<{}>;

const Chat = () => {
  return (
    <div>
      Chat
      <NavbarFooter />
    </div>
  );
};

export default Chat;
